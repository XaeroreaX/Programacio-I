int ingreseNumero(char Mensaje[], int, int);
/** \brief ingresa un numero con un mensaje y luego lo valida con un numero minimo(validarMinimo)
 *
 * \param mensaje que quiera mostrar
 * \param numero a ingresar
 * \param el minimo que tiene el numero
 * \return un valor entero que ingreso
 *
 */
 int validarMinimo(int ,int);
 /** \brief valida enteros solo minimos
  *
  * \param un numero
  * \param y el minimo
  * \return un entero ya validado
  *
  */

  float ingreseNumeroF(char mensaje[], int, int);
  /** \brief ingresa numeros flotantes y luego los valida entre max y min(ValidarMinMax)
   *
   * \param mensaje que pide que tipo de valor quiere que ingrese sueldo, legajo etc
   * \param valor minimo a validar
   * \param valor maximo a validar
   * \return retorna el numero infresado y validado
   */
  float ValidarMinMax(float, int, int);
    /** \brief valida maximos y minimos de numeros flotantes
      * \param numero flotante a validar
     * \param maximo en entero
     * \param minimo en entero
     * \return retorna flotante validado
     *
     */

